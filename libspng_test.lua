local fs = require'fs'
local spng = require'libspng'

--local img = assert(spng.open'')
--local bmp = assert(img:load())

--local f = ''
--spng.save{
--	bitmap = bmp,
--	write = function(buf, sz) return f:write(buf, sz) end,
--}
